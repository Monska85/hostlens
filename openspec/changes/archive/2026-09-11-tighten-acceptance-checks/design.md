# Design

Use the declared case image at the launcher boundary. Verify checksum lists with coreutils inside disposable containers; focused cases expose only their selected archive to the checker. Reuse the existing validation container for archive-only checks. Keep direct launchers single-case. Assert structured MCP observations without dumping payloads. Poll bounded readiness conditions, never retry security assertions. Ship a minimal installation example inheriting runtime defaults.
