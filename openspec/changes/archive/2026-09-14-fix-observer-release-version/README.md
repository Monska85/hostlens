# fix-observer-release-version

Delete the duplicate observerapp.Version literal so hostlens-docker-observer reports contract.Version, and assert the observer version in platform and systemd acceptance.
