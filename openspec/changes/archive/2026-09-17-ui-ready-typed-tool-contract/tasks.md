# Tasks: ui-ready-typed-tool-contract

Work in a branch. Every test runs in the disposable container (`make test-image` once, then `make test`); never run lifecycle tests on the administrator host. Keep the suite green after each numbered group; commit per group with a Conventional Commits subject. Do not check a box until its verification command has passed.

## 0. Baseline and facts

- [x] 0.1 Record the baseline: `make deps && make test-image && make test` green, and note the current statement coverage from `make coverage` (`coverage/functions.txt` total). Verify: numbers written into `docs/v1/VALIDATION.md` draft notes for task 8.4.
- [x] 0.2 In the test container, read the vendored module sources for `modelcontextprotocol/go-sdk@v1.7.0` (`mcp/server.go`, `mcp/tool.go`) and `google/jsonschema-go@v0.4.3` (`jsonschema/infer.go`, struct tag docs) and write `openspec/changes/ui-ready-typed-tool-contract/notes.md` stating: the exact `jsonschema:"..."` tag grammar for minimum, maximum, minLength, description; how `omitempty` and pointers map to `required`; how `map[string]T` maps to `additionalProperties`; whether low-level `Server.AddTool` validates arguments against `InputSchema`; whether `Schema.Resolve` plus `Resolved.Validate` is the validation API. Verify: every statement cites a file and line in the module cache.

## 1. Contract package

- [x] 1.1 Add argument types (`NoArgs`, `PageArgs`, `PathArgs`, `UnitArgs`, `PIDArgs`, `LogsArgs`, `ContainerArgs`, `DockerLogsArgs`) with struct tags per design D3 and a single `finalizeInput` post-processor (`additionalProperties: false`, logs `oneOf`). Verify: `go test ./internal/contract/` includes a test asserting each generated input schema equals the constraints previously produced by `internal/gateway/schema.go` (copy the old expectations into the test before deleting the file).
- [x] 1.2 Produce `openspec/changes/ui-ready-typed-tool-contract/keys-before.txt`: for each tool, the set of JSON member names emitted today under `data` (from `rg 'Data\["' internal/platform/linux`, `dockerobs/projection.go`, and nested map literals such as `checks`, `server`, `limits`). Verify: file lists all 27 tools; it is the parity oracle for tasks 5 and 6.
- [x] 1.3 Add payload types per design D5 and D6 in `contract` (host, audit, logs) and in `dockerobs` (Docker), JSON names exactly as in `keys-before.txt`, optional members as pointers or `omitempty`, no `any` fields. Verify: `go vet ./...` and a `contract` test that walks every registry `Out` type with reflection and fails on any `interface{}` field.
- [x] 1.4 Replace the registry with `Define[In, Out]` entries carrying `In`, `Out`, `InputSchema`, `OutputSchema`, and a unique, specific description per tool; `ValidateRegistry` requires non-nil schemas and unique descriptions; delete `Schema`, its constants, and `Args`; change `Request.Args` to `json.RawMessage` and `Result.Data` to `any`. Verify: `go build ./...` fails only in packages scheduled for tasks 2 to 6, and `go test ./internal/contract/` passes, including a test that the output schema marks `data` optional and enumerates properties for every tool.

- [x] 1.5 Change `get_hostlens_info` `RequiredRole` to `RoleHealth` in the registry; keep `AuditDomain: "hostlens"`. Verify: `go test ./internal/token/ ./internal/gateway/ -race` with new cases: a `health` token discovers and calls `get_hostlens_info` when the `hostlens` audit grant is active, is denied without the grant, and still cannot discover or call any other audit tool; an anonymous request to `/mcp` still receives `401` with an empty body.

## 2. Backend

- [x] 2.1 Decode IPC arguments per tool with `DisallowUnknownFields` into `reflect.New(def.In)`, return `Failure("invalid_arguments")` without admitting a worker on failure, and change `Collector.Collect` to `(ctx, tool string, args any) Result`. Verify: `go test ./internal/backend/ -race` with new cases: unknown member rejected, malformed type rejected, no `Collect` call and no admission slot consumed in either case, valid arguments reach the collector typed.

## 3. Gateway

- [x] 3.1 Register tools through low-level `Server.AddTool` with `InputSchema` and `OutputSchema` from the registry; resolve both schemas once at init; delete `internal/gateway/schema.go`. Verify: `go build ./internal/gateway/` and `tools/list` in `gateway_test.go` shows `outputSchema` with enumerated `data.properties` for every tool.
- [x] 3.2 Validate `tools/call` arguments against the resolved input schema in the existing pre-parse step in `transport.go`, before token re-verification and IPC, returning `isError` with `invalid_arguments`. Verify: `schema_test.go` cases pass unchanged in meaning (empty-object tools reject `path`, logs reject path plus unit, `read_config` requires `path`), plus new cases for unknown member and out-of-range PID, each asserting zero backend requests.
- [x] 3.3 Validate the backend result against the resolved output schema after `Coordinator.Call`; on mismatch return `contract.Failure("response_shape")` with `isError`, and record outcome `response_shape` in the audit record. Verify: new gateway test with a collector that returns a wrong-typed member asserts `isError`, issue code, absent `data`, and the audit outcome; conforming results still pass `gateway_test.go`, `deadline_test.go`, `docker_test.go`.
- [x] 3.4 Confirm the text mirror in `content` equals the marshaled `structuredContent` and that `Result.Bounded` still governs the ceiling with typed payloads. Verify: `transport_limits_test.go` passes; benchmark `metrics_benchmark_test.go` shows no regression beyond noise versus task 0.1.

## 4. Linux collectors: host tools

- [x] 4.1 Convert `get_os_info`, `get_inventory`, `get_health_snapshot`, `list_services`, `get_service_status`, `list_packages`, `read_config`, `query_logs` to typed payloads; replace the post-hoc `r.Data["content"]`-style presence checks with direct `Error` assignment; introduce the generic page helper (design D8). Verify: `go test ./internal/platform/linux/ -race -run 'Collect|Service|Package|Health|Log|Config'` passes with tests adapted to typed payloads, and member names match `keys-before.txt`.

## 5. Linux collectors: audit tools

- [x] 5.1 Convert `list_processes`, `get_process_info`, `get_network_info`, `list_accounts`, `get_storage_info`, `get_update_info`, `get_security_info`, `get_hostlens_info`, `inspect_service`, `inspect_path` to typed payloads; delete `validAuditArgs` and cross-tool argument guards; keep configuration-dependent bounds. Verify: `go test ./internal/platform/linux/ -race -run 'Audit'` passes and the audit evidence check (`scope`/`coverage_complete`-only results are errors) is preserved by a typed test.

## 6. Docker collectors

- [x] 6.1 Replace `dockerobs/projection.go` map builders with typed constructors returning the `dockerobs` payload structs; convert the nine Docker tools and `pagedProjects` to typed pages; keep secret exclusion by construction (no environment, command, label, or health output members exist on the types). Verify: `go test ./internal/dockerobs/ ./internal/platform/linux/ -race -run 'Docker'` passes and Docker member names match `keys-before.txt`.

## 7. Snapshot, smoke tool, dead code

- [x] 7.1 Add the gateway snapshot test per design D9 producing `docs/v1/tools.json` (diagnostics identity, all capabilities true, `read_only` true, sorted by name, two-space indent, trailing newline) with `HOSTLENS_UPDATE_SNAPSHOT=1` regeneration; generate the file. Verify: test fails after editing one description and passes after regeneration; `docs/v1/tools.json` is committed.
- [x] 7.1b Ship the snapshot: `tools/release/prepare.py` copies `docs/v1/tools.json` to `dist/linux-<arch>/tools.json`; `tools/archive/main.go` expected-file list gains `tools.json`; `.github/workflows/release.yml` uploads `dist/linux-amd64/tools.json` as `hostlens-<version>-tools.json` and includes it in `checksums.txt`; `docs/v1/INSTALL.md` extracted-set list names `tools.json`. Verify: `make package && make verify-archives` shows `tools.json` in both archives with identical sha256, and `actionlint` passes on the workflow.
- [x] 7.2 Compare `docs/v1/tools.json` `outputSchema.properties` per tool against `keys-before.txt`. Verify: zero differences, or each difference documented as a defect fixed in tasks 4 to 6 (none expected); delete `keys-before.txt` and `notes.md` from the change directory after this check or move their conclusions into `design.md`.
- [x] 7.3 Adapt `tools/smoke` to the typed envelope (`Result.Data any`), keeping its acceptance assertions. Verify: `go vet ./tools/smoke/` and the smoke tests pass in the container.
- [x] 7.4 Remove dead code left by the refactor: grep for `contract.Args`, `Schema`, `inputSchema`, `validAuditArgs`, `EngineInfoData` and the other `*Data` projections, `[]map[string]any` pages, and unused helpers; confirm no unused exported symbols remain in touched packages. Verify: `rg` for each removed symbol returns nothing, `go vet ./...` clean, `make lint` clean.

## 8. Documentation

- [x] 8.1 `docs/v1/OPERATIONS.md`: add a "Client integration" section covering discovery (`tools/list` with `inputSchema`, `outputSchema`, descriptions), the result envelope members, `isError` and issue codes including `invalid_arguments` and `response_shape`, pagination with `next_offset` and `snapshot_consistent`, the snapshot file, and remote exposure for desktop clients (bind, TLS or trusted proxy, token roles; note that non-browser clients send no `Origin` so `allowed_origins` is not involved). Verify: links resolve via `python3 -B tools/release/check_doc_links.py` (or the repository's documented invocation).
- [x] 8.2 `docs/v1/SPEC.md` "Tool contract": move `get_hostlens_info` to the `health` row of the role table and state that argument and result schemas derive from one typed definition and are validated in both directions; `docs/v1/OPERATIONS.md` "Tokens and roles" and "Generic server and application audits": document the `health`-role availability of `get_hostlens_info` behind the `hostlens` audit grant and state that no unauthenticated endpoint exists; `docs/RELEASING.md`: document the snapshot regeneration command and the shipped `tools.json` archive member and release asset. Verify: `make fmt-check` clean and doc links resolve.
- [x] 8.3 `CHANGELOG.md` Unreleased: Added (per-tool `outputSchema`, `tools.json` in archives and as release asset, specific descriptions, `response_shape` failure), Changed (argument validation derived from schema, `invalid_arguments` before backend contact; `get_hostlens_info` available to the `health` role behind its audit grant; IPC request shape, no client impact), Removed (nothing client-visible). Verify: Keep a Changelog structure preserved.
- [x] 8.4 `docs/v1/VALIDATION.md`: record coverage before and after, the snapshot test, output-validation evidence, and any container kernel or namespace limits observed. Verify: numbers come from the final `make coverage` run.

- [x] 8.5 `AGENTS.md` (and therefore `CLAUDE.md`, which stays a relative symlink): add one bullet under "Implement and validate", placed right after "Keep diagnostics stateless.", with this text: **Define every MCP tool once.** Each tool has one typed Go argument struct and one typed result payload struct in the tool registry; the advertised `inputSchema` and `outputSchema`, gateway validation in both directions, backend argument decoding, and the published contract snapshot all derive from those types. Never hand-write a schema, share an argument union across tools, or build result payloads from untyped maps. Optional observations are pointer or `omitempty` fields, never placeholders; payload fields are never `any`. Keep every JSON member name stable unless the change declares a breaking contract, and regenerate the snapshot through its documented command so drift fails a test. Verify: `CLAUDE.md` still resolves to `AGENTS.md`, the file stays procedural (no version numbers or inventories), and `make fmt-check` is clean.

## 9. Validation

- [x] 9.1 Full repository gate in containers in containers: `make fmt-check`, `make lint`, `make lint-shell` if scripts changed, `make test` (race), `make coverage` at or above the task 0.1 baseline, `make build`, `make package`, `make verify-archives`. Verify: all exit zero; record outputs in the final report.
- [x] 9.2 Container acceptance for the release candidate: `scripts/container-acceptance.sh` (or the `make test-matrix` selection documented in `docs/RELEASING.md`) covering install, token, `tools/list` of the installed candidate compared byte-for-byte (canonical JSON) with `tools.json` extracted from the same archive, one typed call per role, an unknown-argument rejection, and Docker cases where the matrix supports them. Verify: matrix passes and fails when `tools.json` in the archive is deliberately altered; kernel and namespace limits reported honestly.
- [x] 9.3 `openspec validate ui-ready-typed-tool-contract --strict` passes; all tasks above checked only after their verification succeeded. Verify: command output clean; then archive with `openspec archive ui-ready-typed-tool-contract` once the user confirms.
