# unify-fallback-tool-admission

Make the backend-unavailable session fallback use the same fail-closed toolAdmitted predicate as per-call admission; no observable behavior change today, gate proven by test.
