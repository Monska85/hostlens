# accept-case-insensitive-bearer

Parse the Authorization bearer scheme case-insensitively per RFC 6750 in the MCP gateway and the metrics endpoint, keeping single-header, non-empty-token, and status behavior unchanged.
