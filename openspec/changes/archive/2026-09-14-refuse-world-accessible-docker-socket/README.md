# refuse-world-accessible-docker-socket

Require the Docker engine socket to grant no world access at all (root-equivalent exposure), keeping group-write permission, root ownership, and group match checks unchanged.
