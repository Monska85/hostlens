# Design

Use sha256sum, sort and diff to require both archive names and hashes without a checksum parser. Use coreutils timeout for each remaining status-probe budget. Pass a context into HTTP readiness so its existing deadline is testable. Retain failure regressions in the current suites.
