# harden-gateway-unit-sandbox

Bring the gateway systemd unit to the repository sandbox baseline (syscall filter set, personality/realtime/namespace locks, protected clock/hostname/kernel logs, private IPC, hidden proc subsets where evidence allows) and assert the directives in systemd acceptance.
